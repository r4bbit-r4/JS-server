/*
* Same as render.js except it replaces the login button with a profile button
* */
module.exports = function(objectrepository) {

    return function(req, res, next ) {
        return next();
    };

};